# @package _global_

defaults:
  # set up habitat 
  - /env@habitat_baselines: habitat_baselines
  - /env@habitat: habitat 
  - /dataset/imagenav/hm3d_1env
    
  # set up experiment misc.
  - /exp: base
  - /logger@exp.logger: wb
   
  # set up eval
  - /runner@eval: eval
  - /dataset@eval.dataset: offline_episode

  # set up train
  - /runner@train: bc
  - /lr@train.lr_schedule: constant
  - /dataset@train.dataset: offline_episode
  - /models/policy/nav_llama/clip@train.policy: lora 
 
  - _self_


exp:
  name: "lmnav_imagenav_clip_T_22"
  root_dir: "experiments/"
  logger:
    project: "lmnav"
    name: "lmnav imagenav clip trajectory_length=22"
    group: "train"

train:
  batch_size: 2
  epochs: 10_000
  bc_epochs: 10
  grad_accums: 6
  episodes_per_batch: 10
  lr_schedule:
    lr: 2.5e-5
  max_trajectory_length: 22    
  dataset:
    artifact: 
      name: "offline_00744"
      version: "latest"
 
eval:
  policy: ${..train.policy}
  ckpt: "ckpt.*.pth"
  num_episodes: 50
  save_videos: True
  deterministic: False
  num_envs: 10
